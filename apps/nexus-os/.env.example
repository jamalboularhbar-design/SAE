# Nexus OS — environment

# ── Models (OpenAI-compatible). Leave blank to run in DEMO mode. ──
# Google AI (Gemini):  https://generativelanguage.googleapis.com/v1beta/openai
# OpenAI:              https://api.openai.com/v1
LLM_API_URL=
LLM_API_KEY=
LLM_MODEL=gemini-2.5-flash

# ── Hub specialist (jamal-hub-v2) ──
# Optional HTTP endpoint: POST { query, context } -> { answer, citations? }
HUB_SPECIALIST_URL=

# ── Server ──
PORT=8787
